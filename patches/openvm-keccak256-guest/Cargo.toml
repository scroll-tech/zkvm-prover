[package]
name = "openvm-keccak256-guest"
description = "OpenVM guest library for keccak256"
version = "2.0.0"
edition = "2021"
license = "MIT OR Apache-2.0"

# Local experimental fork of openvm `develop-v2.1.0` (29fc511e) keccak256 guest
# crate for zkVM cycle optimization. See patches/ notes in AGENTS.md.

[dependencies]
openvm-platform = { git = "https://github.com/openvm-org/openvm.git", branch = "develop-v2.1.0", default-features = false }

[features]
default = []

